const chai = require('chai');
const expect = chai.expect;
const chaiHttp = reqiure('chai-http');

describe('Test /play endpoint', () => {
    it('what it should do', () => {
        // write the actual test
        let expected = 10;
        let actual = 10;

        expect(actual).to.be.equal(expected);
    })
})